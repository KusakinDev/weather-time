apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: weather-app
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: NodePort  # или LoadBalancer для доступа снаружи